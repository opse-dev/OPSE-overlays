extends _overlay_layout

append main
    .component.fill.b-x.brs-tr(style="top: 10%; left: 20%;")
        p#test TEST

    .component
        p !{SCHOOL_INFO[GENERAL_SETTINGS.GAMES.games[GENERAL_SETTINGS.GAMES.active].away_team].team_name}

append scripts
    script.
        // listening for events
        listen('test', data => {
            $('#test').html(data);
        });